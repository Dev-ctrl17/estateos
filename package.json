{
  "name": "estateos",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "scripts": { "dev": "pnpm --parallel --filter @estateos/web --filter @estateos/api dev", "build": "pnpm -r build", "db:generate": "pnpm --filter @estateos/database generate", "db:migrate": "pnpm --filter @estateos/database migrate" },
  "devDependencies": { "turbo": "^2.3.3", "typescript": "^5.7.2" }
}
