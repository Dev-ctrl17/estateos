import { SocialOAuthService } from "./operations.service";
describe("SocialOAuthService", () => { beforeEach(() => { process.env.SOCIAL_OAUTH_STATE_SECRET = "test-secret"; process.env.LINKEDIN_OAUTH_CLIENT_ID = "client"; process.env.YOUTUBE_OAUTH_CLIENT_ID = "youtube-client"; }); it("creates signed tenant-bound state", () => { const service = new SocialOAuthService(); const state = new URL(service.authorizationUrl("tenant-a", "LINKEDIN", "https://example.test/callback")).searchParams.get("state")!; expect(service.verify(state).tenantId).toBe("tenant-a"); }); it("creates a YouTube Google OAuth URL with upload scope", () => { const service = new SocialOAuthService(); const url = new URL(service.authorizationUrl("tenant-a", "YOUTUBE", "https://example.test/callback")); expect(url.hostname).toBe("accounts.google.com"); expect(url.searchParams.get("access_type")).toBe("offline"); expect(url.searchParams.get("scope")).toContain("youtube.upload"); }); it("rejects altered state", () => { expect(() => new SocialOAuthService().verify("invalid.signature")).toThrow(); }); });
